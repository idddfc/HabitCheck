# 开发日志 — 2026-07-10

## ✅ 已完成

### 🐛 Bug 修复：删除后仍提醒（深入修复）

**问题**：上一次修复将 `cancelNotification` 放在 `deleteHabit`（同步函数）中异步 fire-and-forget，Android 原生取消操作可能未及时完成。

**修复**：将通知取消从数据层移回 UI 层 `TodayScreen`，用 `async/await` 确保原生取消完成后再刷新。

| 文件 | 变更 |
|------|------|
| `src/utils/storage.ts` | 移除 `cancelNotification` 导入和调用，数据层回归纯数据操作 |
| `src/screens/TodayScreen.tsx` | `handleDelete` / `handleArchive` 改为 async，await cancelNotification |

### 🍅 新功能：番茄钟

**存储**：`src/utils/pomodoro.ts` — settings.json + records.json

**界面**：`src/screens/PomodoroScreen.tsx` — 大号倒计时、进度条、今日番茄统计、专注/短休/长休自动切换、震动提醒

**导航**：`App.tsx` 底部 Tab 新增「🍅 番茄钟」

### 📅 新功能：日程事件

**存储**：`src/utils/schedule.ts` — events.json，支持按日期筛选和排序

**界面**：`src/components/AddEventSheet.tsx` — 标题/日期/时间/备注/提醒开关表单

**通知**：`src/utils/notifications.ts` 新增 `scheduleOneTimeNotification`（一次性 DATE trigger）和 `cancelEventNotification`

**日历增强**（`src/screens/CalendarScreen.tsx`）：
- 日程事件在日历上以红色圆点标记
- 点击日期 → 详情中展示当天日程列表（标题+时间）、可删除
- 展示倒计时：显示该日期距离未来每个事件的剩余天数
- 右下角 FAB「+」按钮，快速添加日程

### 📦 版本号

`1.0.0` → `1.1.0`（`package.json` + `app.json`）

## 📋 待办

- [ ] 构建新 APK 进行真机测试
- [ ] 验证番茄钟震动和自动切换
- [ ] 验证日程一次性通知
- [ ] 上传 GitHub

## 🔧 文件变更汇总

| 类型 | 文件 |
|------|------|
| 修改 | `src/utils/storage.ts` `src/screens/TodayScreen.tsx` `src/screens/CalendarScreen.tsx` `src/utils/notifications.ts` `App.tsx` `package.json` `app.json` |
| 新增 | `src/types/index.ts`（新增类型） `src/utils/pomodoro.ts` `src/utils/schedule.ts` `src/screens/PomodoroScreen.tsx` `src/components/AddEventSheet.tsx` `devlog/2026-07-10.md` |
